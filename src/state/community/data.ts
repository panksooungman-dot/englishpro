export type { Notice, FaqItem, Review } from '@/api/_mock/community'
export type { Event } from '@/api/_mock/events'

import { mockNotices, mockFaqs, mockCommunityReviews } from '@/api/_mock/community'
import { mockEvents } from '@/api/_mock/events'

export { mockNotices, mockFaqs, mockCommunityReviews, mockEvents }
